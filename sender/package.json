{
  "name": "sender",
  "version": "1.0.0",
  "description": "Captures local system audio/video and sends via WebRTC",
  "main": "dist/sender.js",
  "type": "module",
  "scripts": {
    "start": "npm run build && node dist/sender.js",
    "build": "tsc"
  },
  "dependencies": {
    "mediasoup-client": "^3.6.69",
    "socket.io-client": "^4.7.5",
    "wrtc": "^0.4.7"
  },
  "devDependencies": {
    "@types/node": "^20.5.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.2"
  }
}
